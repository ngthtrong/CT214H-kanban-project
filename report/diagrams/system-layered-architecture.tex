\begin{figure}[H]
\centering
\begin{tikzpicture}[
    layer/.style={draw,rounded corners,align=center,minimum width=12.8cm,minimum height=1.65cm,font=\small},
    flow/.style={-{Latex[length=2.6mm]},thick}
]
    \node[layer,fill=blue!10] (presentation) at (0,3.0)
    {\textbf{Presentation Layer}\\HTML pages in \code{public/}, JavaScript in \code{js/}, and CSS in \code{css/}};

    \node[layer,fill=green!10] (application) at (0,0.9)
    {\textbf{Application Layer}\\REST endpoints in \code{api/} with business services in \code{src/}};

    \node[layer,fill=orange!12] (data) at (0,-1.2)
    {\textbf{Data Layer}\\PDO helpers in \code{includes/db-connect.php} and MySQL relational schema};

    \draw[flow] (presentation.south) -- node[right]{request handling} (application.north);
    \draw[flow] (application.south) -- node[right]{SQL operations} (data.north);

    \draw[flow] ([xshift=-3.8cm]data.north) -- node[left,font=\scriptsize]{query results} ([xshift=-3.8cm]application.south);
    \draw[flow] ([xshift=3.8cm]application.north) -- node[right,font=\scriptsize]{JSON/view updates} ([xshift=3.8cm]presentation.south);
\end{tikzpicture}
\caption{Three-layer architecture used by Team Kanban}
\label{fig:layered-architecture}
\end{figure}
