\section{Runtime Behavior and Responsive UI}

\subsection{Routing and Runtime}

At runtime, \code{index.php} enforces login state as follows:
\begin{itemize}
    \item Guests are redirected to \code{login.php}.
    \item Authenticated users stay on \code{index.php} (dashboard) and navigate to feature pages such as \code{project.php}, \code{members.php}, \code{join-requests.php}, \code{search.php}, and \code{task.php}.
\end{itemize}

From the dashboard, all major interactions are asynchronous and API-driven.
This design keeps page navigation lightweight while preserving server-side security checks.

\subsection{Responsive Interface}

The UI style system is shared across pages:
\begin{itemize}
    \item \code{css/style.css}: base theme and components.
    \item \code{css/kanban.css}: board-specific visual behavior.
    \item \code{css/responsive.css}: mobile and tablet adaptation.
\end{itemize}

\screenshotplaceholder{Dashboard layout in desktop view}{dashboard-page.png}
\screenshotplaceholder{Kanban board in mobile view}{kanban-mobile-view.png}
