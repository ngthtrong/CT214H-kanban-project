\section{Schema Dictionary}

\begin{longtable}{p{0.18\textwidth} p{0.30\textwidth} p{0.43\textwidth}}
\caption{Main Tables and Key Constraints}\\
\toprule
\textbf{Table} & \textbf{Core Columns} & \textbf{Key Constraints} \\
\midrule
\endfirsthead
\toprule
\textbf{Table} & \textbf{Core Columns} & \textbf{Key Constraints} \\
\midrule
\endhead
\code{users} &
\code{user\_id}, \code{username}, \code{email}, \code{password}, \code{full\_name}, \code{avatar} &
Primary key on \code{user\_id}; unique keys on \code{username} and \code{email}; bcrypt password storage. \\
\code{projects} &
\code{project\_id}, \code{owner\_id}, \code{project\_name}, \code{project\_code}, \code{created\_at} &
Foreign key \code{owner\_id -> users.user\_id}; unique \code{project\_code}; cascade on owner deletion. \\
\code{project\_members} &
\code{member\_id}, \code{project\_id}, \code{user\_id}, \code{role}, \code{joined\_at} &
Foreign keys to projects and users; unique \code{(project\_id, user\_id)} to prevent duplicate membership. \\
\code{project\_join\_requests} &
\code{request\_id}, \code{project\_id}, \code{user\_id}, \code{status}, \code{requested\_at} &
Foreign keys to projects and users; unique \code{(project\_id, user\_id, status)} to avoid duplicate pending requests. \\
\code{tasks} &
\code{task\_id}, \code{project\_id}, \code{assigned\_to}, \code{task\_title}, \code{column\_status}, \code{priority}, \code{due\_date} &
Foreign key \code{project\_id -> projects.project\_id} with cascade; \code{assigned\_to -> users.user\_id} with set-null policy. \\
\bottomrule
\end{longtable}
